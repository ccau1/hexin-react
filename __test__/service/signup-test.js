import assert from 'assert';
import {formValidation} from 'services/SignUp';

// basic testing
describe('formValidation()', function() {
  it('Form Validation', function() {
    assert.equal(true, true);
  });
});
