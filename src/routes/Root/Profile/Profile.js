/* @flow */

import React, {Component} from 'react';

export default class Profile extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div>
        profile page
      </div>
    );
  }
}
